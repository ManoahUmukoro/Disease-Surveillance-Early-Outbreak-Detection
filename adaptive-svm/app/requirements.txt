# Streamlit Community Cloud installs from THIS file: it sits in the same directory
# as the entrypoint (dashboard.py), which Cloud checks before the repo root.
# Dashboard-only subset of ../requirements.txt, pinned to the versions the models
# were trained with so the .pkl bundles unpickle cleanly. Deploy on Python 3.12.
streamlit==1.59.2
scikit-learn==1.9.0
pandas==2.3.3
numpy==1.26.4
scipy==1.17.1
pyreadstat==1.3.5
joblib==1.5.3
pymongo>=4.8,<5     # MongoDB Atlas — unstructured store (optional at runtime, degrades gracefully)
redis>=5,<6         # Redis Streams — ingestion bus (optional at runtime, degrades gracefully)
